@model RealEstateRegalSpace.Models.Reports

@{
    ViewBag.Title = "GetEmpLedger";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}


@using (Html.BeginForm("GetEmpLedger", "Admin", FormMethod.Post, new { @id = "form1", enctype = "multipart/form-data" }))
{
    <div class="wrapper">
        <div class="container-fluid">

            <div class="card-block">
                <!-- Page-Title -->
                <div class="page-title-box">
                    <div class="row align-items-center">
                        <div class="col-sm-6">
                            <h4 class="page-title">Employee Ledger</h4>
                        </div>
                       
                    </div>
                    <!-- end row -->
                </div>
                <div class="form-group row">
                    <div class="col-lg-12">
                        <div class="row">

                            <div class="col-xl-3 col-lg col-md-3 col-sm-3 col-12">
                                <div class="form-group">
                                    <label>Login Id</label>
                                    @Html.TextBoxFor(m => m.LoginId, new { @class = "form-control" })
                                </div>
                            </div>
                            <div class="col-xl-3 col-lg col-md-3 col-sm-3 col-12">
                                <div class="form-group">
                                    <br />
                                    <input type="submit" id="btnSearch" class="btn btn-success pull-left" name="Search" value="Search" onclick="return fvalidate();" />
                                </div>
                            </div>
                        </div>



                        <div class="row gutters table-responsive">
                            <table id="example23" class="table table-bordered">
                                <thead>
                                    <tr>
                                        <th>Sr No.</th>
                                        <th>TransactionDate</th>
                                        <th>Narration</th>
                                        <th>Salary</th>
                                        <th>Advance</th>
                                        <th>Balance</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @if (Model != null)
                                    {
                                        if (Model.lstDetails != null)
                                        {
                                            if (Model.lstDetails.Rows.Count > 0)
                                            {
                                                int j = 1;
                                                for (int i = 0; i <= Model.lstDetails.Rows.Count - 1; i++)
                                                {
                                                    <tr class="text-left">
                                                        <td>@j</td>

                                                        <td>@Model.lstDetails.Rows[i]["TransactionDate"].ToString()</td>
                                                        <td>@Model.lstDetails.Rows[i]["Narration"].ToString()</td>
                                                        <td>@Model.lstDetails.Rows[i]["CrAmount"].ToString()</td>
                                                        <td>@Model.lstDetails.Rows[i]["DrAmount"].ToString()</td>
                                                        <td>@Model.lstDetails.Rows[i]["Balance"].ToString()</td>
                                                    </tr>
                                                    j++;
                                                }
                                            }

                                        }
                                    }
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>

            </div>
        </div>
    </div>
}

<script src="../../Scripts/jquery-1.10.2.min.js"></script>

<script src="//code.jquery.com/ui/1.11.4/jquery-ui.js"></script>
<link rel="stylesheet" type="text/css" href="https://code.jquery.com/ui/1.12.0/themes/smoothness/jquery-ui.css">
<script>

    $('#FromDate').datepicker({
        dateFormat: 'dd/mm/yy',
    });
    $('#Todate').datepicker({
        dateFormat: 'dd/mm/yy',
    });
    function fvalidate() {
        if ($('#LoginId').val() == '') {
            $("#LoginId").addClass('errortext');
            $('#LoginId').focus();

            return false;
        }
        $("#divload").css({ 'display': '' });
    }
</script>



